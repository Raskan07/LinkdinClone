import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Colors from '@/constants/Colors'

const CradVerticalLine = () => {
  return (
    <View style={{width:"80%",marginTop:10,marginBottom:8,height:1,backgroundColor:Colors.default.line}} />
  )
}

export default CradVerticalLine

const styles = StyleSheet.create({})